# Linux Kernel Queue Driver

This repository contains the implementation of a Linux kernel character
device driver that evolves from a minimal loadable module into a
queue-backed device with IOCTL-based interaction.

Kernel-space code lives under the `kernel/` directory.
